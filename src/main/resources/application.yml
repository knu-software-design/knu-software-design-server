openai:
  api:
    key: ${OPEN_AI_API}

server:
  port: ${SPRING_PORT:8080}
  servlet:
    session:
      cookie:
        same-site: none
        secure: true
        http-only: true
        path: /

front:
  base-url: ${FRONT_BASEURL:http://localhost:5173}

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:30005/swProject_db}
    username: ${DB_USERNAME:swProject}
    password: ${DB_PASSWORD:asdf}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 15
      minimum-idle: 15
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
        jdbc:
          time_zone: Asia/Seoul
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    root: INFO
  file:
    path: ./logs
    name: app.log

springdoc:
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  cache:
    disabled: true
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  swagger-ui:
    enabled: true
    path: /swagger
    tags-sorter: alpha
    operations-sorter: alpha
